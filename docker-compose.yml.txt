version: '3'
services:
  backend:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
  frontend:
    image: node:18-alpine
    working_dir: /client
    volumes:
      - ./client:/client
    command: sh -c "npm install && npm start"
    ports:
      - "3000:3000"
